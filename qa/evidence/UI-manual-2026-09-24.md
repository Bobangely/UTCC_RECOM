# UI Manual Execution Evidence — 2026-09-24

Browser: Codex in-app browser

Application: `http://localhost:8080`

## Observed Results

- Home page loaded with HTTP 200 and displayed UTCCRECOM content.
- Nearby page loaded with HTTP 200.
- Home-to-Nearby navigation changed the URL to `/nearby.html`.
- Dark Mode toggle changed the document theme to `dark`.
- EN language button changed the main heading to “Discover Interesting Places Around UTCC”.
- Dark Mode and EN language remained selected after navigation to the Nearby page.
- Nearby map displayed UTCC marker and zoom controls.
- Nearby fallback dataset displayed 19 places when available in browser memory.
- Search term `สุกี้` returned one result: `สุกี้นายพัน`.
- Search term `ไม่มีสถานที่ชื่อนี้` returned `0 สถานที่` and the empty-state message.
- The main page displayed an empty-data message when its live API returned HTTP 500; it did not clearly distinguish server failure from a genuine empty result.

No create/update/delete action was performed because the datasource was unavailable. This avoided leaving partial test records in the shared database.
